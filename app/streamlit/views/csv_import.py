"""CSV Import page (placeholder — FIN-02 structure only).

Will eventually let the user upload a CSV file matching the locked
format in API_CONTRACT.md §5 and surface the backend's per-row errors.
"""

import streamlit as st


def render() -> None:
    st.title("CSV Import")
    st.info("Placeholder page — CSV import content is not implemented yet.")
